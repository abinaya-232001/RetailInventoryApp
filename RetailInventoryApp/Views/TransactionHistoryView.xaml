<UserControl x:Class="RetailInventoryApp.Views.TransactionHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Transaction History" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="BtnExportCsv" Content="Export CSV" Style="{StaticResource PrimaryButtonStyle}" Click="BtnExportCsv_Click"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="6" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="All Transactions" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,10"/>
                    <TextBox x:Name="TxtSearch" Grid.Row="1" Padding="8" Margin="0,0,0,10" TextChanged="TxtSearch_TextChanged"
                             Tag="Search by transaction # or date (yyyy-MM-dd)..."/>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <DatePicker x:Name="DpFrom" Width="130" SelectedDateChanged="DateRange_Changed"/>
                        <TextBlock Text="To:" VerticalAlignment="Center" Margin="14,0,6,0"/>
                        <DatePicker x:Name="DpTo" Width="130" SelectedDateChanged="DateRange_Changed"/>
                        <Button x:Name="BtnClearDates" Content="Clear Dates" Padding="8,4" Margin="14,0,0,0" Click="BtnClearDates_Click"/>
                    </StackPanel>
                    <Grid Grid.Row="3">
                        <DataGrid x:Name="GridTransactions" AutoGenerateColumns="False" IsReadOnly="True"
                                  HeadersVisibility="Column" GridLinesVisibility="None" RowHeight="32"
                                  SelectionMode="Single" SelectionChanged="GridTransactions_SelectionChanged"
                                  BorderThickness="0" CanUserAddRows="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="50"/>
                                <DataGridTextColumn Header="Date &amp; Time" Binding="{Binding TransactionDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="150"/>
                                <DataGridTextColumn Header="Items" Binding="{Binding ItemCount}" Width="60"/>
                                <DataGridTextColumn Header="Total (Rs.)" Binding="{Binding TotalAmount, StringFormat=N2}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock x:Name="TxtNoTransactions" Text="No transactions match the current filter."
                                   Foreground="Gray" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,40,0,0"
                                   Visibility="Collapsed"/>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="6" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" x:Name="TxtDetailTitle" Text="Select a transaction to view details" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,10"/>
                    <DataGrid Grid.Row="1" x:Name="GridDetailItems" AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="None" RowHeight="32"
                              BorderThickness="0" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                            <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=N2}" Width="90"/>
                            <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat=N2}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock Grid.Row="2" x:Name="TxtDetailTotal" Margin="0,10,0,0" FontWeight="Bold" FontSize="16" HorizontalAlignment="Right"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
